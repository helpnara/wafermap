[pytest]
# rootdir를 명시해 두면 어느 하위 폴더에서 pytest를 실행하든 같은 설정이 적용된다.
testpaths = tests

# src 레이아웃 — 이 줄이 있으면 `pytest`와 `python -m pytest` 어느 쪽으로 실행해도
# wafermap 패키지를 찾는다. (conftest.py의 sys.path 조작과 중복이지만,
# 설정으로 명시해 두는 편이 실행 방식에 따라 달라지는 사고를 막는다.)
pythonpath = src

addopts = --strict-markers
