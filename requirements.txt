# ── 실행에 필요한 최소 의존성 ────────────────────────────────────────────
# Streamlit Community Cloud(RAM ~1GB, CPU only) 배포를 전제로 버전을 고정한다.
# torch는 CPU wheel만 사용하며, M2(패턴 CNN) 단계에서 추가한다.

# 앱
streamlit>=1.40,<2.0        # st.navigation(position="hidden") 필요 → 1.36+ (§4A.1)

# 데이터
numpy>=1.26,<3.0
pandas>=2.1,<3.0
pyarrow>=14.0               # parquet I/O

# 피처 추출 (M2)
scikit-image>=0.22          # Radon 변환, 연결성분, Hu moment
scipy>=1.11                 # 통계 검정, 신호처리

# 모델링 (M2~M4)
scikit-learn>=1.4
lightgbm>=4.1

# 통계 분석 (M3)
statsmodels>=0.14           # BH-FDR 다중검정 보정, CMH 검정

# 해석 (M4)
shap>=0.44

# 시각화
plotly>=5.18
matplotlib>=3.8
