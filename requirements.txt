# ── 실행에 필요한 최소 의존성 ────────────────────────────────────────────
# Streamlit Community Cloud(RAM ~1GB, CPU only) 배포를 전제로 버전을 고정한다.
# torch는 CPU wheel만 사용하며, M2(패턴 CNN) 단계에서 추가한다.

# 앱
streamlit>=1.40,<2.0        # st.navigation(position="hidden") 필요 → 1.36+ (§4A.1)

# 데이터
numpy>=1.26,<3.0
pandas>=2.1,<3.0
pyarrow>=14.0               # parquet I/O

# 피처 추출 (M2)
scikit-image>=0.22          # Radon 변환, 연결성분, Hu moment
scipy>=1.11                 # 통계 검정, 신호처리

# 모델링 (M2~M4)
scikit-learn>=1.4
lightgbm>=4.1

# 딥러닝 비교군 (M2) — CPU 전용 휠을 쓸 것
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# PyPI 기본 휠은 CUDA 의존성까지 끌어와 약 5GB가 된다. CPU 휠은 약 200MB이며
# Streamlit Community Cloud 배포 요건(§4.3)에도 이쪽만 들어간다.
torch>=2.0

# 통계 분석 (M3)
statsmodels>=0.14           # BH-FDR 다중검정 보정, CMH 검정

# 해석 (M4)
shap>=0.44

# 시각화
plotly>=5.18
matplotlib>=3.8
